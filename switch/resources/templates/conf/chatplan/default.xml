<?xml version="1.0" encoding="utf-8"?>
<include>
        <context name="default">
                <extension name="demo">
                        <condition field="to" expression="^(.*)$">
                                <!-- <action application="python" data="test"/> -->
                                <!-- <action application="reply" data="Hello, you said: ${_body}"/> -->
                        </condition>
                </extension>
        </context>   
        <context name="public">
                <extension name="demo">
                        <condition field="to" expression="^(.*)$">
                                <!-- <action application="python" data="sms outbound"/> -->
                        </condition>
                </extension>
        </context>
</include>
